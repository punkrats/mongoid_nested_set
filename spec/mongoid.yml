test:
  clients:
    default:
      database: mongoid_nested_set_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1
        wait_queue_timeout: 5
